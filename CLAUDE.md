# Shift Management Calendar

Single `index.html` app — no build step, no dependencies, no external URLs.
GitHub: `git@github.com:fearlessresponsesolution/calendar.git` (SSH, push to `main`)
GitHub Pages: https://fearlessresponsesolution.github.io/calendar/

**Core constraint:** Everything lives in `index.html`. Never add imports, CDN links, or external URLs.

See `.claude/rules/` for architecture, data model, critical rules, code patterns, known issues, and do-nots.
